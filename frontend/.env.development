# HMR-Konfiguration wird jetzt über CRACO (craco.config.js) gesteuert
# Die CRACO-Konfiguration behebt die HMR-Endlosschleife durch optimierte Webpack-Einstellungen
# FAST_REFRESH kann wieder aktiviert bleiben, da CRACO das Problem löst

# Fast Refresh (HMR) aktiviert - wird durch CRACO optimiert
FAST_REFRESH=true

#!/bin/bash
# Deployment-Trigger über MCP-System
# Die Konfiguration kommt aus mcp.json (Cursor verwaltet das automatisch)

echo "🚀 Starte Deployment über MCP-System..."
echo "⚠️  Bitte verwende das MCP Tool 'deploy_to_production' mit confirm: true"
echo ""
echo "Die Konfiguration ist bereits in mcp.json vorhanden."
echo "Das MCP-System wird automatisch die richtige Konfiguration verwenden."
